# direnv：进入本目录时自动执行 `nix develop`，离开时还原环境。
#
# 首次使用：direnv allow .
# 若系统装有 nix-direnv，则自动获得 devShell 缓存与 GC root 管理；
# 否则使用 direnv 内置的 `use flake`（需要 nix-command / flakes 实验特性）。
if ! has nix_direnv_version 2>/dev/null; then
  watch_file flake.nix
  watch_file flake.lock
fi

use flake
